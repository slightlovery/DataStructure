#include"LinkedList.h"

/*
//链表
void main() {

	
	int n, i, e;
	char tag;
	Status result;

	printf("----------创建单链表----------\n");
	printf("请输入元素的个数:");
	cin >> n;
	LinkList L;
	result = createLinkList(L, n);
	cout << "操作结果为：" << result << endl;
	Print(L);

	printf("----------取第i个元素----------\n");
	printf("请输入要取的元素的位置i:\n");
	scanf_s("%d", &i);
	result = GetElem_L(L, i , e);
	cout << "操作结果为：" << result << endl;
	Print(L);

	printf("----------插入----------\n");
	printf("请输入插入的位置和插入的元素:\n");
	scanf_s("%d %d", &i, &e);
	result = InsertLinkList(L, i, e);
	cout << "操作结果为：" << result << endl;
	Print(L);

	printf("----------删除----------\n");
	printf("请输入删除元素的位置:\n");
	cin >> i;
	result = deleteLinkList(L, i);
	cout << "操作结果为：" << result <<endl;
	Print(L);

	cout << "程序结束。";
	cin >> tag;
	
}

*/