#include"Prim.h"

/*
int main()
{
	adjmatrix GA;

	Edge GE[n*(n - 1) / 2], T[n];

	CreatMatrix(GA);

	//InitEdge(GE, arcnum);

	//GetEdgeSet(GA, GE);

	//SortEdge(GE, arcnum);

	Prim(GA, T);

	printf("\n");

	OutEdge(T, n - 1);

	int data;
	cin >> data;
	system("pause");

	return 0;
}
*/